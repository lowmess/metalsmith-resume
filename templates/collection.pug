extends default

block content

  h2= title
  if title == 'Experience'
    for job in collections.experience
      +experience(job)
  else if title == 'Education'
    for school in collections.education
      +experience(school)

  footer
    hr
    p: a(href="/") Back to Full Resume
