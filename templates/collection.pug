extends default

block content

  h3.f5.fw7.mv2.ttu= title
  if title == 'Experience'
    for job in collections.experience
      +experience(job)
  else if title == 'Education'
    for school in collections.education
      +experience(school)

  footer.mt3
    hr.w5.ba.b--accent.ml0
    p: a.link.dark-gray.underline-hover(href='/') Back to Full Resume
